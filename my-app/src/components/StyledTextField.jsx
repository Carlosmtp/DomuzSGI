import React from 'react'
import { styled, TextField } from '@mui/material'

const Style = styled(TextField)({
    margin:"normal"
})

const StyledTextField = () => {
  return (
    <Style />
  )
}

export default StyledTextField